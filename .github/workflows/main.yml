name: autoblack

on: [push, pull_request]

jobs:
  black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@26.3.1
        with:
          options: "--check --verbose --target-version=py311"
          src: "."
          version: 26.3.1
